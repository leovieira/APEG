grammar ForLoop extends {Expression, Identifier}
   Expr |:=
      for { i:Id <- e:Expr ,? SPACE }* do block:Expr end =>
      <[ for2 i**; e **;. do block; end ]>
     | for2 i:Id* ; e:Expr => <[ boom ]> 

   test ::= a => <[ b ]>

end
