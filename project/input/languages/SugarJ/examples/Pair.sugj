package pair;

import org.sugarj.languages.Java;
import concretesyntax.Java;

public sugar Pair {
  context-free syntax
     LPAR type COMMA type RPAR -> type;
     LPAR expression COMMA expression RPAR -> expression;
}